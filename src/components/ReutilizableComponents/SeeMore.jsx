import React from "react";

const SeeMore = ({state, handler}) => {
//todo: agregar ver mas al texto y tipado
  return (
    <p
      onClick={handler}
      className="text-darkLight text-sm cursor-pointer text-nowrap inline-block"
    >
das    </p>
  );
};

export default SeeMore
